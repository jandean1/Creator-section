{
  "name": "industrial-telemetry-dashboard",
  "version": "2.1.0",
  "description": "A secure, production-grade static interface for monitoring fluid dynamics and thermal telemetry analytics.",
  "private": true,
  "license": "MIT",
  "scripts": {
    "dev": "netlify dev",
    "build": "mkdir -p public && cp index.html public/index.html && echo 'Asset compilation successful.'"
  },
  "dependencies": {
    "normalize.css": "^8.0.1"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  },
  "keywords": [
    "telemetry",
    "engineering",
    "dashboard",
    "static-site"
  ]
}
